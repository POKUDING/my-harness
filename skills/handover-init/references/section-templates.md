# Section Templates — 인수인계 절 마크다운 골격

`/handover-init` Step 5에서 각 절 본문을 생성할 때 이 reference의 골격을 따른다. 17개 절(A~Q) 각각:

- 헤딩 구조 (h1 + h2 sub-section)
- 인터뷰 빈 칸 항목 (Step 4에서 채움)
- Mermaid placeholder (Step 5에서 자동 시도, 실패 시 `<!-- 자동 생성 실패 -->`)

---

## A. 프로젝트 개요 & 전체 구조도

# 01. 프로젝트 개요 & 전체 구조도

## 1. 서비스 개요

**{서비스명}**은 {1줄 설명}.

- **클라이언트**: {클라이언트 목록}
- **백엔드**: {프레임워크 + 버전}
- **DB**: {DB + 버전}
- **인증**: {인증 방식}
- **비동기**: {큐/워커}
- **스케줄러**: {cron 시스템}
- **실시간**: {WS/SSE}
- **푸시**: {Push 서비스}
- **이메일**: {Email 서비스}
- **파일 업로드**: {파일 패턴}

## 2. 도메인 모듈 맵

<!-- AI-generated — 검토 필요 -->
```mermaid
graph LR
    {자동 생성: 폴더 구조 → subgraph}
```

## 3. 시스템 전체 구조도

<!-- AI-generated — 검토 필요 -->
```mermaid
graph TB
    {자동 생성: 클라우드 리소스 → 노드}
```

**인터뷰 빈 칸**: 서비스명, 1줄 설명, 클라이언트 종류 (자동 추출 못 했을 때).

---

## B. 폴더 구조 / 모듈 책임 매핑

# 05. 폴더 구조 & 모듈 책임 매핑

## 1. 루트 폴더 구조

```
{프로젝트 루트}/
├── {자동 추출 top-level 디렉토리}
└── ...
```

## 2. 핵심 모듈 책임

| 모듈 | 경로 | 책임 |
|------|------|------|
| {자동 추출} | {경로} | {SDK·imports 기반 추론 또는 [확인 필요]} |

**인터뷰 빈 칸**: 각 모듈의 책임 (자동 추론 실패 시).

---

## C. 로컬 개발 환경

# 12. 로컬 개발 환경

## 1. 사전 요구사항

- **{언어}**: {버전}
- **패키지 매니저**: {pnpm/uv/poetry/...}
- **DB**: {로컬 DB 필요 여부}

## 2. 설치

```bash
{자동 추출: README의 설치 명령 또는 표준 명령}
```

## 3. 실행

```bash
{자동 추출: package.json scripts 또는 manage.py runserver 등}
```

## 4. 주의사항

{인터뷰 답변 또는 [확인 필요]}

**인터뷰 빈 칸**: 자주 발생하는 setup 함정, 로컬 도메인 hosts 설정 등.

---

## D. CI/CD 파이프라인

# 02. CI/CD 파이프라인

## 1. 트리거 정책

{자동 추출: workflows YAML의 on 섹션 파싱}

## 2. 빌드 → 테스트 → 배포 흐름

<!-- AI-generated — 검토 필요 -->
```mermaid
graph LR
    {자동 생성: workflows의 jobs 시퀀스}
```

## 3. 배포 환경

| 환경 | 브랜치 | 배포 대상 |
|------|--------|----------|
| {자동 추출} | ... | ... |

**인터뷰 빈 칸**: 수동 승인 단계, 배포 권한, 롤백 절차.

---

## E. 인프라 구조

# 03. 인프라 구조

## 1. 클라우드 / 리전

- **클라우드**: {AWS / GCP / ...}
- **리전**: {자동 추출 또는 [확인 필요]}

## 2. 컴퓨트

| 서비스 | 종류 | 비고 |
|--------|------|------|
| {AWS ECS list-services 결과} | Fargate / EC2 | ... |

## 3. 네트워크 / DB / 저장소

<!-- AI-generated — 검토 필요 -->
```mermaid
graph TB
    {자동 생성: VPC → ALB → ECS → RDS/S3 등}
```

**인터뷰 빈 칸**: 비용 책임, 알람 채널, 인프라 코드 위치.

---

## F. Secrets / 환경변수 관리

# 04. Secrets / 환경변수 관리

## 1. 환경변수 소스

| 환경 | 소스 |
|------|------|
| local | `.env` |
| dev/prod | {AWS Secrets Manager / 다른 서비스} |

## 2. 주요 환경변수

| 키 | 용도 | 비고 |
|----|------|------|
| {.env.example 자동 추출} | ... | [확인 필요] |

## 3. Secret 이름 목록 (값 제외)

| 이름 | 환경 |
|------|------|
| {aws secretsmanager list-secrets 결과 또는 [확인 필요]} | ... |

**인터뷰 빈 칸**: secret 갱신 주기, 권한 부여 절차.

---

## G. 외부 API 통합

# 06. 외부 API 통합

## 1. 통합 목록

| 서비스 | SDK | 용도 | 인증 방식 |
|--------|-----|------|----------|
| {자동 추출: HTTP 클라이언트 + 알려진 SDK} | ... | ... | ... |

## 2. 호출 흐름

<!-- AI-generated — 검토 필요 -->
```mermaid
sequenceDiagram
    {자동 생성: 클라이언트 → 외부 API 시퀀스}
```

**인터뷰 빈 칸**: 각 API의 운영자 연락처, rate limit, 장애 fallback.

---

## H. 알림 (Push/Email/SMS)

# 07. 통합 알림 시스템

## 1. 채널 목록

| 채널 | 서비스 | 트리거 위치 |
|------|--------|-------------|
| Push | {Expo / FCM / ...} | ... |
| Email | {SES / Mailgun / ...} | ... |
| SMS | {Coolsms / Twilio / ...} | ... |

## 2. 통합 흐름

<!-- AI-generated — 검토 필요 -->
```mermaid
graph LR
    {자동 생성: 이벤트 → notification module → 채널 분기}
```

**인터뷰 빈 칸**: 알림 권한·정책, 사용자 옵트아웃 처리.

---

## I. DB 스키마 & 마이그레이션

# 08. DB 스키마 / 마이그레이션

## 1. 사용 DB

- **종류**: {PostgreSQL / MySQL / MongoDB / ...}
- **버전**: {자동 추출 또는 [확인 필요]}
- **ORM**: {Django / Prisma / TypeORM / ...}

## 2. 마이그레이션 도구

{자동 추출: makemigrations / prisma migrate / alembic / ...}

## 3. 핵심 테이블

| 테이블 | 모듈 | 비고 |
|--------|------|------|
| {자동 추출: models 파일 파싱} | ... | ... |

**인터뷰 빈 칸**: prod 마이그레이션 절차, downtime 회피, 백업 정책.

---

## J. 인증 / 보안

# 09. 인증 / 보안

## 1. 사용자 모델

| 모델 | 종류 | 인증 방식 |
|------|------|----------|
| {자동 추출: User/Account 모델} | ... | JWT / OAuth / ... |

## 2. 토큰 흐름

<!-- AI-generated — 검토 필요 -->
```mermaid
sequenceDiagram
    {자동 생성: 클라이언트 → /auth/login → JWT 시퀀스}
```

## 3. 권한 모델

| 권한 | 부여 조건 | 비고 |
|------|----------|------|
| {자동 추출 또는 [확인 필요]} | ... | ... |

**인터뷰 빈 칸**: 보안 사고 대응 절차, OAuth 클라이언트 발급자.

---

## K. 스케줄러 / Cron

# 10. 스케줄러 & Cron 잡

## 1. 스케줄러 종류

- **종류**: {celery beat / EventBridge / node-cron / BullMQ / ...}
- **위치**: {자동 추출}

## 2. Cron 잡 목록

| 잡 이름 | 주기 | 위치 |
|--------|------|------|
| {자동 추출: 코드 또는 EventBridge rules} | ... | ... |

**인터뷰 빈 칸**: 잡 실패 시 알림 채널, 재실행 절차.

---

## L. 실시간 통신

# 11. 실시간 통신 (WebSocket / SSE)

## 1. 사용 기술

- **종류**: {WebSocket / SSE / API Gateway WS / Channels / Socket.io}
- **연결 경로**: {자동 추출}

## 2. 라우팅

<!-- AI-generated — 검토 필요 -->
```mermaid
graph LR
    {자동 생성: 클라이언트 → WS handler → backend}
```

## 3. 인증

{자동 추출 또는 [확인 필요]}

**인터뷰 빈 칸**: 연결 끊김 처리, scale-out 전략.

---

## M. 운영 체크리스트

# 13. 운영 체크리스트

## 1. 배포

- 배포 절차: [확인 필요 — 인터뷰]
- 롤백: [확인 필요 — 인터뷰]
- 배포 시 주의사항: [확인 필요 — 인터뷰]

## 2. 장애 대응

- 자주 발생하는 장애: [확인 필요 — 인터뷰]
- 온콜 연락처: [확인 필요 — 인터뷰]

## 3. 알람 / 모니터링

{자동 추출 또는 [확인 필요]}

**인터뷰 빈 칸**: 배포 절차, 롤백, 장애 패턴, 운영 담당자, 주의사항.

---

## N. 프로덕션 접근 (Bastion/SSH/VPN)

# 14. 프로덕션 접근

## 1. 접근 경로

- **종류**: {Bastion EC2 / SSM Session Manager / VPN / ...}
- **자격증명 발급**: [확인 필요 — 인터뷰]

## 2. DB 접근

```bash
{자동 추출: SSM 포트포워딩 명령 또는 [확인 필요]}
```

**인터뷰 빈 칸**: 자격증명 발급 절차, 일회용 접근 정책.

---

## O. Admin / 백오피스

# 15. Admin / 백오피스

## 1. 위치

- **URL**: {자동 추출: admin host 또는 [확인 필요]}
- **권한 모델**: {자동 추출 또는 [확인 필요]}

## 2. 주요 기능

{자동 추출: admin.py / 라우트 파싱}

**인터뷰 빈 칸**: 슈퍼유저 발급, 권한 정책.

---

## P. 프론트엔드 빌드/배포

# 프론트엔드 빌드 & 배포

## 1. 빌드 도구

- **번들러**: {자동 추출: Vite / Webpack / Next.js / ...}
- **배포 대상**: {Vercel / Netlify / S3+CloudFront / ...}

## 2. 빌드 명령

```bash
{자동 추출: package.json scripts}
```

## 3. 환경별 도메인

| 환경 | 도메인 |
|------|--------|
| {[확인 필요 — 인터뷰]} | ... |

**인터뷰 빈 칸**: 환경별 도메인, CDN 정책.

---

## Q. 모바일 빌드/배포

# 모바일 빌드 & 배포

## 1. 플랫폼

- **종류**: {Expo / React Native / Flutter / Native iOS / Native Android}
- **빌드 시스템**: {EAS / Fastlane / Bitrise / ...}

## 2. 빌드 명령

```bash
{자동 추출: eas.json / fastlane / package.json scripts}
```

## 3. 스토어 배포

| 스토어 | 배포 절차 |
|--------|----------|
| App Store | [확인 필요 — 인터뷰] |
| Play Store | [확인 필요 — 인터뷰] |

**인터뷰 빈 칸**: 스토어 계정 책임자, 코드 사인 절차.

---

## 사용 규칙

- 각 절 본문은 위 골격을 그대로 따른다.
- 자동 추출 못한 자리는 `[확인 필요]` 또는 인터뷰 답변으로 채운다.
- Mermaid 다이어그램은 모두 `<!-- AI-generated — 검토 필요 -->` 주석을 부착한다.
- 자동 생성 실패 시 `<!-- 자동 생성 실패 — 수동 추가 권장. (이유: <간단> ) -->` 명시.
