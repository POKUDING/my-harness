# Vercel Provider — Stub (Future Work)

이 provider는 아직 구현되지 않았습니다.

자격증명 감지(`vercel whoami`) 시도 시 즉시 skip하고 사용자 인터뷰로 fallback합니다.

## TODO (future work)

- `vercel whoami` Detect 명령
- Scope Map: 절 E·P (프론트엔드 배포)
- `vercel projects ls`, `vercel domains ls`, `vercel env ls` 화이트리스트
- Output Mapping (절 P 중심)
