# GCP Provider — Stub (Future Work)

이 provider는 아직 구현되지 않았습니다.

자격증명 감지(`gcloud auth list`) 시도 시 즉시 skip하고 사용자 인터뷰로 fallback합니다.

## TODO (future work)

- `gcloud auth list` Detect 명령
- Scope Map: 절 E·F·N
- Cloud Run / GKE / Cloud SQL / Secret Manager 명령 화이트리스트
- Output Mapping
