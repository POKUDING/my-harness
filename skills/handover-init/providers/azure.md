# Azure Provider — Stub (Future Work)

이 provider는 아직 구현되지 않았습니다.

자격증명 감지(`az account show`) 시도 시 즉시 skip하고 사용자 인터뷰로 fallback합니다.

## TODO (future work)

- `az account show` Detect 명령
- Scope Map: 절 E·F·N
- App Service / AKS / Azure SQL / Key Vault 명령 화이트리스트
- Output Mapping
