# Netlify Provider — Stub (Future Work)

이 provider는 아직 구현되지 않았습니다.

자격증명 감지(`netlify status`) 시도 시 즉시 skip하고 사용자 인터뷰로 fallback합니다.

## TODO (future work)

- `netlify status` Detect 명령
- Scope Map: 절 E·P
- `netlify sites:list`, `netlify env:list` 화이트리스트
- Output Mapping (절 P 중심)
